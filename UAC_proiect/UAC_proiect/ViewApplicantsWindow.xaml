<Window x:Class="UAC_proiect.ViewApplicantsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UAC_proiect"
        mc:Ignorable="d"
        Title="ViewApplicantsWindow" Height="500" Width="1000">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock" Grid.Row="0" Text="Applicants" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="AcceptButton" Content="Accept" Width="120" Height="30" Margin="0,0,10,0" Click="AcceptButton_Click"/>
            <Button x:Name="RejectButton" Content="Reject" Width="120" Height="30" Margin="0,0,10,0" Click="RejectButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Width="120" Height="30" Click="CloseButton_Click"/>
        </StackPanel>
        <DataGrid x:Name="ApplicantsGrid" Grid.Row="2" AutoGenerateColumns="True" SelectionMode="Single" SelectionUnit="FullRow"/>
    </Grid>
</Window>